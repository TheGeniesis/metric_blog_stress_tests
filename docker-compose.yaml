version: '2'
services:
  node:
    image: node:10.16
    working_dir: /app
    volumes:
      - ./:/app
    networks:
      - metric_article_network

networks:
  metric_article_network:
    external: true
